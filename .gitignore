node_modules
dist
.env
.env.local
.DS_Store

# texture calibrator render snapshots (dev)
snapshots/
.snapshots/

# A live session saved by `check:smoke -- --login`. Never commit this.
.smoke-session.json
